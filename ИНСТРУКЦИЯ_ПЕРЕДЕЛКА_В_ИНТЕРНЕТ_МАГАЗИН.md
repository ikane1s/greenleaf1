# Инструкция по переделке иллюстрационного сайта в полноценный интернет-магазин

## Оглавление

1. [Обзор изменений](#обзор-изменений)
2. [Бэкенд разработка](#бэкенд-разработка)
3. [Фронтенд изменения](#фронтенд-изменения)
4. [Интеграция платежных систем](#интеграция-платежных-систем)
5. [Система управления заказами](#система-управления-заказами)
6. [Административная панель](#административная-панель)
7. [Безопасность](#безопасность)
8. [Тестирование](#тестирование)
9. [Деплой и запуск](#деплой-и-запуск)

---

## Обзор изменений

### Текущее состояние

- Статический каталог товаров (JSON файл)
- Формы обратной связи (callback, партнеры)
- Отображение товаров без возможности покупки
- Нет корзины, оформления заказов, оплаты

### Целевое состояние

- Полноценный интернет-магазин с:
  - Корзиной покупок
  - Оформлением заказов
  - Интеграцией платежных систем
  - Личным кабинетом пользователя
  - Административной панелью
  - Управлением товарами и заказами
  - Системой доставки
  - Уведомлениями

---

## Бэкенд разработка

### 1. База данных

#### Необходимые таблицы:

**users** (Пользователи)

```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  first_name VARCHAR(100),
  last_name VARCHAR(100),
  phone VARCHAR(20),
  role VARCHAR(20) DEFAULT 'customer', -- customer, partner, admin
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**products** (Товары)

```sql
CREATE TABLE products (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  category VARCHAR(100) NOT NULL,
  image_url VARCHAR(500),
  price_retail DECIMAL(10, 2) NOT NULL,
  price_partner DECIMAL(10, 2),
  pv_points DECIMAL(10, 2),
  stock_quantity INTEGER DEFAULT 0,
  is_active BOOLEAN DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**cart_items** (Корзина)

```sql
CREATE TABLE cart_items (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  product_id INTEGER NOT NULL,
  quantity INTEGER NOT NULL DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id),
  FOREIGN KEY (product_id) REFERENCES products(id)
);
```

**orders** (Заказы)

```sql
CREATE TABLE orders (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  order_number VARCHAR(50) UNIQUE NOT NULL,
  total_amount DECIMAL(10, 2) NOT NULL,
  status VARCHAR(50) DEFAULT 'pending', -- pending, paid, processing, shipped, delivered, cancelled
  payment_method VARCHAR(50),
  payment_status VARCHAR(50) DEFAULT 'unpaid', -- unpaid, paid, refunded
  shipping_address TEXT,
  shipping_method VARCHAR(100),
  notes TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

**order_items** (Товары в заказе)

```sql
CREATE TABLE order_items (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  order_id INTEGER NOT NULL,
  product_id INTEGER NOT NULL,
  quantity INTEGER NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  total DECIMAL(10, 2) NOT NULL,
  FOREIGN KEY (order_id) REFERENCES orders(id),
  FOREIGN KEY (product_id) REFERENCES products(id)
);
```

**addresses** (Адреса доставки)

```sql
CREATE TABLE addresses (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  type VARCHAR(20) DEFAULT 'shipping', -- shipping, billing
  first_name VARCHAR(100),
  last_name VARCHAR(100),
  phone VARCHAR(20),
  address_line1 VARCHAR(255),
  address_line2 VARCHAR(255),
  city VARCHAR(100),
  region VARCHAR(100),
  postal_code VARCHAR(20),
  country VARCHAR(100) DEFAULT 'Russia',
  is_default BOOLEAN DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### 2. API Endpoints

#### Аутентификация

```
POST /api/auth/register - Регистрация пользователя
POST /api/auth/login - Вход в систему
POST /api/auth/logout - Выход
GET /api/auth/me - Получить текущего пользователя
```

#### Товары

```
GET /api/products - Список товаров (с фильтрацией и пагинацией)
GET /api/products/:id - Детали товара
GET /api/products/categories - Список категорий
```

#### Корзина

```
GET /api/cart - Получить корзину пользователя
POST /api/cart/add - Добавить товар в корзину
PUT /api/cart/update/:id - Обновить количество
DELETE /api/cart/remove/:id - Удалить товар из корзины
DELETE /api/cart/clear - Очистить корзину
```

#### Заказы

```
POST /api/orders - Создать заказ
GET /api/orders - Список заказов пользователя
GET /api/orders/:id - Детали заказа
PUT /api/orders/:id/status - Обновить статус заказа (admin)
```

#### Адреса

```
GET /api/addresses - Список адресов пользователя
POST /api/addresses - Добавить адрес
PUT /api/addresses/:id - Обновить адрес
DELETE /api/addresses/:id - Удалить адрес
```

#### Платежи

```
POST /api/payments/create - Создать платеж
POST /api/payments/webhook - Webhook от платежной системы
GET /api/payments/:id/status - Статус платежа
```

### 3. Технологии для бэкенда

**Рекомендуемый стек:**

- **Node.js + Express** (текущий стек) или **Python + Django/FastAPI**
- **PostgreSQL** или **MySQL** вместо SQLite для продакшена
- **JWT** для аутентификации
- **bcrypt** для хеширования паролей
- **Sequelize** или **TypeORM** для работы с БД
- **Redis** для кеширования и сессий
- **Nodemailer** для отправки email уведомлений

---

## Фронтенд изменения

### 1. Новые компоненты

#### Компонент корзины

```jsx
// src/components/Cart/Cart.jsx
- Отображение товаров в корзине
- Изменение количества
- Удаление товаров
- Подсчет общей суммы
- Кнопка "Оформить заказ"
```

#### Компонент оформления заказа

```jsx
// src/pages/Checkout/Checkout.jsx
- Форма доставки
- Выбор способа доставки
- Выбор способа оплаты
- Подтверждение заказа
```

#### Личный кабинет

```jsx
// src/pages/Account/Account.jsx
- Профиль пользователя
- История заказов
- Адреса доставки
- Настройки
```

#### Компонент авторизации

```jsx
// src/components/Auth/Login.jsx
// src/components/Auth/Register.jsx
- Форма входа
- Форма регистрации
- Восстановление пароля
```

### 2. State Management

**Рекомендуется использовать:**

- **Redux Toolkit** или **Zustand** для глобального состояния
- **React Query** для работы с API и кеширования

**Основные слайсы:**

- `authSlice` - аутентификация
- `cartSlice` - корзина
- `productsSlice` - товары
- `ordersSlice` - заказы

### 3. Обновление существующих компонентов

#### Header

- Добавить иконку корзины с количеством товаров
- Добавить кнопку входа/личный кабинет
- Уведомления о новых заказах

#### ProductCard

- Кнопка "Добавить в корзину"
- Отображение наличия товара
- Быстрый просмотр

#### Catalog

- Фильтр по наличию товара
- Сортировка по наличию

---

## Интеграция платежных систем

### 1. Выбор платежной системы

**Рекомендуемые для России:**

- **ЮKassa** (Яндекс.Касса) - популярная, хорошая документация
- **CloudPayments** - простая интеграция
- **PayPal** - для международных платежей
- **Stripe** - для международных платежей

### 2. Процесс оплаты

1. Пользователь оформляет заказ
2. Создается платеж в системе
3. Пользователь перенаправляется на страницу оплаты
4. После оплаты - webhook уведомляет бэкенд
5. Статус заказа обновляется на "оплачен"
6. Отправка уведомления пользователю

### 3. Пример интеграции ЮKassa

```javascript
// backend/routes/payments.js
const yookassa = require("yookassa")({
  shopId: process.env.YOOKASSA_SHOP_ID,
  secretKey: process.env.YOOKASSA_SECRET_KEY,
});

app.post("/api/payments/create", async (req, res) => {
  const { orderId, amount, description } = req.body;

  const payment = await yookassa.createPayment(
    {
      amount: {
        value: amount,
        currency: "RUB",
      },
      confirmation: {
        type: "redirect",
        return_url: `${process.env.FRONTEND_URL}/orders/${orderId}`,
      },
      description: description,
    },
    orderId
  );

  res.json({ paymentUrl: payment.confirmation.confirmation_url });
});
```

---

## Система управления заказами

### 1. Статусы заказа

- **pending** - Ожидает оплаты
- **paid** - Оплачен
- **processing** - В обработке
- **shipped** - Отправлен
- **delivered** - Доставлен
- **cancelled** - Отменен

### 2. Уведомления

**Email уведомления:**

- Подтверждение заказа
- Подтверждение оплаты
- Отправка заказа
- Доставка заказа
- Отмена заказа

**SMS уведомления (опционально):**

- Использовать сервисы: Twilio, SMS.ru, SMSC

### 3. Трекинг заказов

- Генерация трек-номера
- Интеграция с транспортными компаниями (СДЭК, Почта России)
- Отображение статуса доставки

---

## Административная панель

### 1. Функционал

**Управление товарами:**

- CRUD операции с товарами
- Загрузка изображений
- Управление остатками
- Массовые операции

**Управление заказами:**

- Просмотр всех заказов
- Изменение статусов
- Печать накладных
- Экспорт данных

**Управление пользователями:**

- Просмотр пользователей
- Изменение ролей
- Блокировка/разблокировка

**Аналитика:**

- Статистика продаж
- Популярные товары
- Выручка по периодам

### 2. Технологии

- **React Admin** или **Ant Design Pro** для быстрой разработки
- Или создать собственную панель на React

---

## Безопасность

### 1. Аутентификация и авторизация

- JWT токены с коротким временем жизни
- Refresh tokens
- Защита от CSRF атак
- Rate limiting на API endpoints

### 2. Защита данных

- HTTPS обязателен
- Хеширование паролей (bcrypt)
- Валидация всех входных данных
- SQL injection защита (использовать ORM)
- XSS защита

### 3. PCI DSS (для платежей)

- Не хранить данные карт на сервере
- Использовать токенизацию
- Следовать стандартам PCI DSS

---

## Тестирование

### 1. Unit тесты

- Тестирование бизнес-логики
- Тестирование утилит
- Покрытие кода > 70%

### 2. Integration тесты

- Тестирование API endpoints
- Тестирование интеграций с платежными системами

### 3. E2E тесты

- Тестирование полного цикла покупки
- Использовать Cypress или Playwright

---

## Деплой и запуск

### 1. Инфраструктура

**Рекомендуемые сервисы:**

- **Vercel/Netlify** - для фронтенда
- **Railway/Render** - для бэкенда
- **Supabase/PlanetScale** - для базы данных
- **Cloudflare** - для CDN и защиты

### 2. CI/CD

- Автоматический деплой при пуше в main ветку
- Автоматические тесты перед деплоем
- Staging окружение для тестирования

### 3. Мониторинг

- **Sentry** - отслеживание ошибок
- **Google Analytics** - аналитика
- **LogRocket** - мониторинг сессий

---

## Пошаговый план внедрения

### Фаза 1: Базовая функциональность (2-3 недели)

1. Настройка базы данных
2. Реализация аутентификации
3. Компонент корзины
4. Базовое оформление заказа

### Фаза 2: Платежи и заказы (2-3 недели)

1. Интеграция платежной системы
2. Система управления заказами
3. Email уведомления
4. Личный кабинет

### Фаза 3: Админ-панель (2-3 недели)

1. Управление товарами
2. Управление заказами
3. Аналитика

### Фаза 4: Оптимизация и тестирование (1-2 недели)

1. Оптимизация производительности
2. Тестирование
3. Исправление багов
4. Подготовка к запуску

---

## Дополнительные рекомендации

### SEO оптимизация

- Мета-теги для каждой страницы
- Структурированные данные (Schema.org)
- Sitemap.xml
- Robots.txt

### Производительность

- Ленивая загрузка изображений
- Кеширование API запросов
- Оптимизация изображений (WebP)
- Code splitting

### Мобильная версия

- PWA функциональность
- Push уведомления
- Офлайн режим

### Многоязычность (опционально)

- i18n для поддержки нескольких языков
- Локализация валют и дат

---

## Полезные ресурсы

- [Документация ЮKassa](https://yookassa.ru/developers)
- [React Query документация](https://tanstack.com/query/latest)
- [Redux Toolkit документация](https://redux-toolkit.js.org/)
- [Stripe документация](https://stripe.com/docs)

---

## Контакты и поддержка

При возникновении вопросов или проблем в процессе внедрения, обращайтесь к команде разработки.

**Удачи в создании интернет-магазина! 🚀**
