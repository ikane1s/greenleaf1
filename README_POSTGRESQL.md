# Миграция на PostgreSQL - Краткая инструкция

## ✅ Что было сделано

1. **Удален SQLite** из зависимостей (`sqlite3` удален из `package.json`)
2. **Настроен PostgreSQL** в `backend/models.js`
3. **Обновлена инструкция** по деплою на Railway

## 🚀 Быстрый старт

### Для локальной разработки:

1. **Установите PostgreSQL:**

   - Windows: https://www.postgresql.org/download/windows/
   - macOS: `brew install postgresql && brew services start postgresql`
   - Linux: `sudo apt-get install postgresql postgresql-contrib`

2. **Создайте базу данных:**

   ```bash
   createdb greenleaf_db
   ```

3. **Создайте файл `backend/.env`:**

   ```env
   DATABASE_URL=postgresql://localhost:5432/greenleaf_db
   PORT=3001
   BOT_TOKEN=ваш_токен_telegram_бота
   CHAT_ID=ваш_chat_id
   NODE_ENV=development
   ```

4. **Установите зависимости:**

   ```bash
   cd backend
   npm install
   ```

5. **Запустите сервер:**
   ```bash
   npm start
   ```

### Для деплоя на Railway:

1. Создайте PostgreSQL базу данных в Railway (см. подробную инструкцию в `ИНСТРУКЦИЯ_ДЕПЛОЙ_RAILWAY.md`)
2. Railway автоматически добавит `DATABASE_URL` в переменные окружения
3. Деплойте бэкенд как обычно

## 📝 Важные изменения

- **`backend/models.js`** - теперь использует PostgreSQL с автоматическим определением необходимости SSL
- **`backend/package.json`** - удалена зависимость `sqlite3`
- Все остальное работает так же, как и раньше

## ⚠️ Важно

- Файл `.env` не должен попадать в Git
- Убедитесь, что PostgreSQL запущен перед запуском бэкенда
- Для Railway: `DATABASE_URL` будет добавлен автоматически при создании PostgreSQL сервиса
