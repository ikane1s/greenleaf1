# Подробная инструкция по деплою React + Express + SQLite на Railway

## 📋 Содержание

1. [Подготовка проекта](#подготовка-проекта)
2. [Создание аккаунта на Railway](#создание-аккаунта-на-railway)
3. [Настройка базы данных](#настройка-базы-данных)
4. [Деплой бэкенда](#деплой-бэкенда)
5. [Деплой фронтенда](#деплой-фронтенда)
6. [Настройка переменных окружения](#настройка-переменных-окружения)
7. [Подключение домена](#подключение-домена)
8. [Важные замечания](#важные-замечания)

---

## 🚀 Подготовка проекта

### 1. Структура проекта

Убедитесь, что ваш проект имеет следующую структуру:

```
greenleaf1/
├── backend/
│   ├── index.js
│   ├── models.js
│   ├── package.json
│   └── database.sqlite (будет создана автоматически)
├── frontend/
│   ├── src/
│   ├── public/
│   └── package.json
└── railway.json (создадим)
```

### 2. Обновление package.json для бэкенда

Убедитесь, что в `backend/package.json` есть скрипт `start`:

```json
{
  "name": "greenleaf-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "sequelize": "^6.32.1",
    "sqlite3": "^5.1.6",
    "node-telegram-bot-api": "^0.61.0",
    "dotenv": "^16.3.1"
  }
}
```

### 3. Обновление package.json для фронтенда

В `frontend/package.json` должен быть скрипт `build`:

```json
{
  "name": "greenleaf-frontend",
  "scripts": {
    "build": "react-scripts build",
    "start": "react-scripts start"
  }
}
```

### 4. Обновление API_URL во фронтенде

Создайте файл `frontend/src/config.js`:

```javascript
// config.js
const API_URL = process.env.REACT_APP_API_URL || 'http://localhost:3001/api';
export default API_URL;
```

Затем обновите все места, где используется `http://localhost:3001/api`:

В `frontend/src/pages/Partners.jsx`:

```javascript
import API_URL from '../../config';
// Удалите строку: const API_URL = 'http://localhost:3001/api';
```

В `frontend/src/components/Callback/CallbackModal.jsx`:

```javascript
import API_URL from '../../config';
// Замените 'http://localhost:3001/api/callback' на `${API_URL}/callback`
```

### 5. Создание railway.json (опционально)

Создайте файл `railway.json` в корне проекта:

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  }
}
```

---

## 🔐 Создание аккаунта на Railway

### Шаг 1: Регистрация

1. Перейдите на https://railway.app
2. Нажмите "Start a New Project"
3. Войдите через GitHub (рекомендуется) или email
4. Railway **НЕ блокирует** российские IP-адреса и домены

### Шаг 2: Создание проекта

1. Нажмите "New Project"
2. Выберите "Empty Project"
3. Назовите проект (например, "greenleaf")

---

## 💾 Настройка базы данных

### PostgreSQL на Railway

Проект использует PostgreSQL. Railway предоставляет бесплатный PostgreSQL:

1. В проекте Railway нажмите "+ New"
2. Выберите "Database" → "Add PostgreSQL"
3. Railway автоматически создаст базу данных
4. Скопируйте переменную окружения `DATABASE_URL` (Railway предоставит её автоматически)

**Важно:** `DATABASE_URL` будет автоматически добавлен в переменные окружения всех сервисов в проекте Railway.

### Локальная разработка с PostgreSQL

Для локальной разработки вам нужно установить PostgreSQL:

1. **Установите PostgreSQL:**

   - Windows: https://www.postgresql.org/download/windows/
   - macOS: `brew install postgresql`
   - Linux: `sudo apt-get install postgresql`

2. **Создайте базу данных:**

   ```bash
   createdb greenleaf_db
   ```

3. **Создайте файл `.env` в папке `backend/`:**

   Создайте файл `backend/.env` со следующим содержимым:

   ```env
   DATABASE_URL=postgresql://localhost:5432/greenleaf_db
   PORT=3001
   BOT_TOKEN=ваш_токен_telegram_бота
   CHAT_ID=ваш_chat_id
   NODE_ENV=development
   ```

   **Важно:** Файл `.env` не должен попадать в Git. Убедитесь, что он добавлен в `.gitignore`.

---

## 🔧 Деплой бэкенда

### Шаг 1: Подготовка бэкенда для Railway

Обновите `backend/index.js` для работы с Railway:

```javascript
// В начале файла добавьте:
const PORT = process.env.PORT || 3001;

// В конце файла измените:
app.listen(PORT, '0.0.0.0', () => {
  console.log(`🚀 Server running on port ${PORT}`);
});
```

### Шаг 2: Деплой через GitHub

1. **Создайте репозиторий на GitHub:**

   ```bash
   cd greenleaf1
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/greenleaf.git
   git push -u origin main
   ```

2. **Подключите GitHub к Railway:**

   - В проекте Railway нажмите "+ New"
   - Выберите "GitHub Repo"
   - Выберите ваш репозиторий
   - Railway автоматически определит проект

3. **Настройте Root Directory:**

   - В настройках сервиса найдите "Root Directory"
   - Установите: `backend`

4. **Настройте Build Command:**

   - Railway автоматически установит зависимости
   - Если нужно, добавьте: `npm install`

5. **Настройте Start Command:**
   - Установите: `npm start`

### Шаг 3: Настройка переменных окружения для бэкенда

В настройках сервиса бэкенда найдите "Variables" и добавьте:

```
PORT=3001
BOT_TOKEN=ваш_токен_telegram_бота
CHAT_ID=ваш_chat_id
NODE_ENV=production
```

Если используете PostgreSQL, добавьте:

```
DATABASE_URL=postgresql://... (Railway предоставит это автоматически)
```

### Шаг 4: Получение URL бэкенда

После деплоя Railway предоставит URL вида:

```
https://ваш-проект-production.up.railway.app
```

Скопируйте этот URL - он понадобится для фронтенда.

---

## 🎨 Деплой фронтенда

### Шаг 1: Обновление конфигурации

В `frontend/package.json` добавьте `homepage`:

```json
{
  "homepage": ".",
  "scripts": {
    "build": "react-scripts build"
  }
}
```

### Шаг 2: Создание статического сервера для фронтенда

Создайте файл `frontend/server.js`:

```javascript
import express from 'express';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const app = express();
const PORT = process.env.PORT || 3000;

// Раздаем статические файлы из build
app.use(express.static(path.join(__dirname, 'build')));

// Все остальные запросы отправляем на index.html
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, 'build', 'index.html'));
});

app.listen(PORT, '0.0.0.0', () => {
  console.log(`Frontend server running on port ${PORT}`);
});
```

Обновите `frontend/package.json`:

```json
{
  "scripts": {
    "build": "react-scripts build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
```

### Шаг 3: Деплой фронтенда

1. **В Railway создайте новый сервис:**

   - Нажмите "+ New" в проекте
   - Выберите "GitHub Repo"
   - Выберите тот же репозиторий

2. **Настройте Root Directory:**

   - Установите: `frontend`

3. **Настройте Build Command:**

   ```
   npm install && npm run build
   ```

4. **Настройте Start Command:**

   ```
   npm run serve
   ```

   **Важно:** Используйте `npm run serve` для продакшена, а не `npm start` (который для разработки).

### Шаг 4: Настройка переменных окружения для фронтенда

В настройках сервиса фронтенда добавьте:

```
REACT_APP_API_URL=https://ваш-бэкенд-url.up.railway.app/api
PORT=3000
NODE_ENV=production
```

---

## 🌐 Подключение домена

### Важно: Railway НЕ блокирует российские домены!

В отличие от Vercel, Railway работает с любыми доменами без VPN.

### Шаг 1: Получение домена

1. Купите домен у любого регистратора (например, reg.ru, timeweb.ru)
2. Или используйте бесплатный домен от Freenom

### Шаг 2: Настройка DNS

1. В настройках сервиса Railway найдите "Settings" → "Domains"
2. Нажмите "Custom Domain"
3. Введите ваш домен (например, `greenleaf.ru`)
4. Railway предоставит CNAME запись вида:
   ```
   cname.railway.app
   ```

### Шаг 3: Настройка DNS у регистратора

В панели управления доменом добавьте CNAME запись:

**Тип:** CNAME  
**Имя:** @ (или www для поддомена)  
**Значение:** `cname.railway.app` (Railway предоставит точное значение)  
**TTL:** 3600

### Шаг 4: Ожидание распространения DNS

- Обычно занимает 5-30 минут
- Проверить можно через: https://dnschecker.org

### Шаг 5: SSL сертификат

Railway автоматически выдаст SSL сертификат через Let's Encrypt после настройки DNS.

---

## ⚙️ Настройка переменных окружения

### Бэкенд (Backend Service)

```
PORT=3001
BOT_TOKEN=ваш_токен_telegram_бота
CHAT_ID=ваш_chat_id
NODE_ENV=production
DATABASE_URL=postgresql://... (Railway предоставит автоматически при создании PostgreSQL)
```

**Важно:** `DATABASE_URL` будет автоматически добавлен Railway при создании PostgreSQL базы данных. Вам не нужно добавлять его вручную.

### Фронтенд (Frontend Service)

```
REACT_APP_API_URL=https://ваш-бэкенд-url.up.railway.app/api
PORT=3000
NODE_ENV=production
```

**Важно:** Все переменные, начинающиеся с `REACT_APP_`, доступны во фронтенде.

---

## 🔍 Важные замечания

### 1. PostgreSQL на Railway

- PostgreSQL база данных предоставляется бесплатно на Railway
- Данные сохраняются между перезапусками
- Автоматические бэкапы включены
- `DATABASE_URL` автоматически добавляется в переменные окружения

### 2. Локальная разработка

Для локальной разработки установите PostgreSQL и создайте файл `.env` в папке `backend/`:

```env
DATABASE_URL=postgresql://localhost:5432/greenleaf_db
PORT=3001
BOT_TOKEN=ваш_токен
CHAT_ID=ваш_chat_id
NODE_ENV=development
```

### 3. Ограничения бесплатного плана Railway

- 500 часов работы в месяц
- 5$ кредитов в месяц
- Достаточно для небольших проектов

### 4. Мониторинг и логи

- В Railway есть встроенный просмотр логов
- Доступен мониторинг использования ресурсов

### 5. Автоматический деплой

Railway автоматически деплоит при каждом push в main ветку GitHub.

### 6. Безопасность

- Никогда не коммитьте `.env` файлы
- Используйте переменные окружения Railway
- Храните секреты в Railway Variables

---

## 📝 Чек-лист деплоя

- [ ] Проект загружен на GitHub
- [ ] Бэкенд настроен и работает локально
- [ ] Фронтенд настроен и работает локально
- [ ] API_URL обновлен во фронтенде
- [ ] Создан проект на Railway
- [ ] Бэкенд задеплоен
- [ ] Фронтенд задеплоен
- [ ] Переменные окружения настроены
- [ ] Домен подключен (опционально)
- [ ] SSL сертификат получен
- [ ] Тестирование на продакшене

---

## 🆘 Решение проблем

### Проблема: Бэкенд не запускается

**Решение:**

- Проверьте логи в Railway
- Убедитесь, что PORT берется из переменных окружения
- Проверьте, что все зависимости установлены

### Проблема: Фронтенд не может подключиться к бэкенду

**Решение:**

- Проверьте REACT_APP_API_URL
- Убедитесь, что CORS настроен правильно
- Проверьте, что бэкенд доступен по URL

### Проблема: База данных не работает

**Решение:**

- Проверьте, что `DATABASE_URL` установлен в переменных окружения
- Убедитесь, что PostgreSQL сервис создан в Railway
- Проверьте логи подключения к БД в Railway
- Для локальной разработки: убедитесь, что PostgreSQL запущен (`pg_isready`)

### Проблема: Домен не работает

**Решение:**

- Проверьте DNS записи через dnschecker.org
- Убедитесь, что CNAME запись правильная
- Подождите распространения DNS (до 24 часов)

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу:

- Railway URL: `https://ваш-проект.up.railway.app`
- Или ваш домен: `https://ваш-домен.ru`

**Railway НЕ блокирует российские IP и домены**, в отличие от Vercel!
