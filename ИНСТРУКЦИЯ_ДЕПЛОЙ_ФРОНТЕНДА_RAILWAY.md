# Инструкция по деплою фронтенда на Railway

## ⚠️ Проблема с npm ci

Если вы видите ошибку `npm ci` при деплое, это обычно означает:
1. Проблемы с `package-lock.json`
2. Неправильный Build Command
3. Отсутствие зависимостей

## ✅ Правильная настройка фронтенда в Railway

### Шаг 1: Настройка сервиса

1. **Root Directory:** `frontend`
2. **Build Command:** `npm install && npm run build`
   - Используйте `npm install` вместо `npm ci` для избежания проблем
3. **Start Command:** `npm run serve`

### Шаг 2: Переменные окружения

В разделе "Variables" добавьте:

```
REACT_APP_API_URL=https://ваш-бэкенд-url.up.railway.app/api
PORT=3000
NODE_ENV=production
```

**Важно:** Замените `https://ваш-бэкенд-url.up.railway.app/api` на реальный URL вашего бэкенда.

### Шаг 3: Проверка package.json

Убедитесь, что в `frontend/package.json` есть:

```json
{
  "homepage": ".",
  "dependencies": {
    "express": "^4.18.2",
    ...
  },
  "scripts": {
    "build": "react-scripts build",
    "serve": "node server.js"
  }
}
```

### Шаг 4: Альтернативный вариант (если npm install не работает)

Если `npm install` тоже не работает, попробуйте:

**Build Command:**
```bash
npm install --legacy-peer-deps && npm run build
```

Или:

**Build Command:**
```bash
rm -rf node_modules package-lock.json && npm install && npm run build
```

## 🔍 Диагностика проблем

### Проблема: npm ci показывает usage/help

**Решение:** Используйте `npm install` вместо `npm ci` в Build Command.

### Проблема: Ошибка при установке зависимостей

**Решение:** 
1. Проверьте, что `package-lock.json` закоммичен в Git
2. Используйте `npm install --legacy-peer-deps`
3. Убедитесь, что версия Node.js соответствует `engines` в package.json

### Проблема: Build не создается

**Решение:**
1. Проверьте логи сборки
2. Убедитесь, что `REACT_APP_API_URL` установлен
3. Проверьте, что нет ошибок в коде React

## 📝 Чек-лист

- [ ] Root Directory установлен в `frontend`
- [ ] Build Command: `npm install && npm run build`
- [ ] Start Command: `npm run serve`
- [ ] `REACT_APP_API_URL` установлен в Variables
- [ ] `express` добавлен в dependencies
- [ ] `homepage: "."` добавлен в package.json
- [ ] `server.js` существует в папке frontend

